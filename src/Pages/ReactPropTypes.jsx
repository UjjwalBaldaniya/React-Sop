import React from "react";
import ReactPropTypesT from "./ReactPropTypesT";

const ReactPropTypes = () => {
    const name = "ujjwal"
    const age = 50

    return (
        <>
            <ReactPropTypesT name={name} age={age} />
        </>
    )
};

export default ReactPropTypes;
